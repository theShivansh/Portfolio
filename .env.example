# Canonical site URL (no trailing slash). Vercel production URL is used if unset.
NEXT_PUBLIC_SITE_URL=

# Path to a resume PDF in /public, e.g. /resume.pdf. Resume links are hidden until set.
NEXT_PUBLIC_RESUME_PATH=

# Full LinkedIn profile URL. Hidden until set.
NEXT_PUBLIC_LINKEDIN_URL=
